# Bucle while

# Ejemplo: programa que imprime los 10 primeros múltiplos de los 10 primeros números

int i = 1;
while (i < 11) {
    int j = 1;
    while (j < 11) {
        int mul = i * j;
        print(mul);
        j = j + 1;
    }
    i = i + 1;
}